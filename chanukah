def numofcan(specs):
    candles = specs[0]
    days = specs[1]
    
    needcan = days + ((days * (days + 1))/2)
    print ('{0} {1}' .format(candles, int(needcan)))
n = int(input())
for i in range(n):
    _specs = [int (j) for j in input ().split()]
    numofcan(_specs)
